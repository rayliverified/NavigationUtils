name: example_auth
description: A new Flutter project.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  firebase_core: ^2.15.0
  firebase_auth: ^4.7.1
  google_sign_in: ^6.1.4 # Missing Windows, macOS Support.
  cloud_firestore: ^4.8.3 # Missing Window Support.
  firebase_database: ^10.2.4 # Missing Windows Support.
  firebase_storage: ^11.2.5 # Missing Windows Support.
  cloud_functions: ^4.3.4 # Missing Windows Support.
  firebase_analytics: ^10.4.4
  firebase_dynamic_links: ^5.3.4
  firebase_crashlytics: ^3.3.4
  navigation_utils:
    path: ../
  get_it: ^7.6.0
  equatable: ^2.0.5
  simple_gravatar: ^1.1.0
  universal_io: ^2.2.2
  json_annotation: ^4.8.1
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.2
  json_serializable: ^6.7.1
  build_runner: ^2.4.6

flutter:
  uses-material-design: true
  assets:
    - assets/
    - assets/images/