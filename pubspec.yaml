name: navigation_utils
description: The missing navigation library for Navigator 2.
version: 0.0.1-pre.2
homepage: https://codelessly.com
repository: https://github.com/rayliverified/NavigationUtils

environment:
  sdk: ">=2.17.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.1
